{
  "name": "@drakkar.software/whistlers",
  "version": "0.1.0",
  "description": "Message-queue to push-notification bridge",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "nats": "^2.29.0",
    "mqtt": "^5.10.1"
  },
  "peerDependencies": {
    "firebase-admin": "^12.0.0 || ^13.0.0"
  },
  "peerDependenciesMeta": {
    "firebase-admin": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "firebase-admin": "^13.4.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
